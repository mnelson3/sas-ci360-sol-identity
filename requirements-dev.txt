# Dependencies needed to lint, type-check, and test this package.
#
# Deliberately excludes sasci360apicore/sasci360apiscim (private packages
# requiring an internal index) and saspy/pandas/schedule/DateTime (unused by
# this package directly). Unit tests mock the private encryption dependency
# instead of installing it. See requirements.txt for the full production set.
requests>=2.31.0
urllib3>=2.0.0
pytest>=9.1.1
pytest-cov>=4.1.0
flake8>=6.1.0
mypy>=1.5.0
types-requests>=2.31.0
